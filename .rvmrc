
if [[ -d "${rvm_path:-$HOME/.rvm}/environments" \
  && -s "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.2-p180@berlinracingteam" ]] ; then
  \. "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.2-p180@berlinracingteam"
else
  rvm --create  "ruby-1.9.2-p180@berlinracingteam"
fi

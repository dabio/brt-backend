<div id="sponsors">

  <header class="units-row">
    <h2 class="unit-66"><%= title %></h2>
    <nav class="unit-33 secondary">
      <form action="<%= Sponsor.createlink %>" method="post">
        <button class="btn btn-blue icon-plus">Neuer Sponsor</button>
      </form>
    </nav>
  </header>

  <table class="width-100">

    <thead>
      <tr>
        <th>Name</ht>
        <th colspan="2">Homepage</th>
      </tr>
    </thead>

    <tbody>

      <% if sponsors.each do |sponsor| %>

        <tr>
          <td><%= sponsor.title %></td>
          <td>
            <a href="<%= sponsor.url %>"><%= sponsor.url %></a>
          </td>
          <td>
            <a class="icons" href="<%= sponsor.editlink %>" title="Bearbeiten">&#x21;</a>
          </td>
        </tr>

      <% end.empty? %>

        <tr>
          <td colspan="3">Keine Sponsoren vorhanden</td>
        </tr>

      <% end %>

    </tbody>

  </table>

</div>

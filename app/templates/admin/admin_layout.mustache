<!DOCTYPE html>
<html>
<head>
  <meta charset="utf8">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="/css/kube.min.css">
  <link rel="stylesheet" href="/css/master.css">
</head>

<body>

  <header></header>

  <section class="container row">
    <nav class="threequarter">
      {{#navigation}}
        <a href="{{href}}" title="{{title}}">{{title}}</a>
      {{/navigation}}
    </nav>
    <nav class="quarter" style="text-align: right">
      <a href="{{current_person.editlink}}" title="Einstellungen">Einstellungen</a>
      <a href="/logout" title="Abmelden">Abmelden</a>
    </nav>
    </nav>
  </section>

  {{>_flash}}

  <section class="container">
    {{{yield}}}
  </section>


  {{{javascripts}}}

  {{>_tracking }}
</body>
</html>
